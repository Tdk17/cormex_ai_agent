name: agente_vendas_saas
description: Front-end Flutter do Agente de Vendas SaaS multi-tenant.
publish_to: none
version: 0.6.0+8

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  csv: ^8.0.0
  dio: ^5.11.0
  file_picker: ^11.0.3
  flutter_secure_storage: ^11.0.0
  get_it: ^9.2.1
  go_router: ^17.5.0
  intl: ^0.20.3
  signals: ^7.1.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
